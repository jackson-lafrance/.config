Secondary Monitor bars.
The two monitors and the left and right should ahve simpler more nonchalant widgets

left to right
active workspace number
more nonchalant version of hardware stats
current volume level, network connected to, and io audio device (first actually build this out into the main volume widget, remove the vol/vol icon text and replace it with the current audio output and in the volume selection window allow the user to change output and input devices)
The time and date of the month

anything else you think would be cool
These should all be built in accordance with the current widgets style and code structure
